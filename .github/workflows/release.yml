# Release Workflow - Publish all packages on GitHub Release
#
# Publishes:
#   - 5 TypeScript packages to npm (OIDC provenance)
#   - 2 Python packages to PyPI (OIDC trusted publishing)
#   - Docker multi-platform image to GHCR
#
# Triggered ONLY on GitHub Release events.
# OIDC trusted publishers are configured on PyPI and npm — no token secrets needed.

name: Release

on:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write
  packages: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

# =============================================================================
# npm publish - TypeScript packages (OIDC provenance)
# =============================================================================
jobs:
  verify:
    name: Verify release inputs
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - uses: pnpm/action-setup@v6

      - uses: astral-sh/setup-uv@v7

      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install JS dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Python dependencies
        run: uv sync --all-packages --all-extras

      - name: Verify TypeScript
        run: pnpm nx run-many -t lint,build,typecheck,test:coverage --projects=tag:lang:ts

      - name: Verify Python
        run: pnpm nx run-many -t lint,typecheck,test --projects=tag:lang:py

      - name: Verify docs build and tests
        run: |
          pnpm --dir docs run build
          pnpm --dir docs run test

      - name: Install Playwright browsers
        run: pnpm --dir docs exec playwright install --with-deps chromium

      - name: Verify docs e2e
        run: pnpm --dir docs run test:e2e --project=chromium

  npm-publish:
    name: 'npm: ${{ matrix.name }}'
    runs-on: ubuntu-latest
    needs: verify
    strategy:
      fail-fast: false
      matrix:
        include:
          - dir: packages/agentic
            name: '@jbcom/agentic'
          - dir: packages/triage
            name: '@jbcom/agentic-triage'
          - dir: packages/meshy-content-generator
            name: '@jbcom/agentic-meshy'
          - dir: packages/providers
            name: '@jbcom/agentic-providers'
          - dir: tooling/vitest-agentic
            name: '@jbcom/vitest-agentic'
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build package
        run: pnpm nx build
        working-directory: ${{ matrix.dir }}

      - name: Publish to npm
        run: |
          npm publish --access public --provenance --no-git-checks 2>&1 | tee /tmp/npm-publish.log; rc=${PIPESTATUS[0]}
          if [ $rc -ne 0 ]; then
            if grep -qiE 'EPUBLISHCONFLICT|already exists|Cannot publish over|previously published' /tmp/npm-publish.log; then
              echo "Package version already published — skipping."
            else
              exit $rc
            fi
          fi
        working-directory: ${{ matrix.dir }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  # ===========================================================================
  # PyPI publish - Python packages (OIDC trusted publishing)
  # ===========================================================================
  pypi-publish:
    name: 'pypi: ${{ matrix.name }}'
    runs-on: ubuntu-latest
    needs: verify
    strategy:
      fail-fast: false
      matrix:
        include:
          - dir: packages/agentic-crew
            name: agentic-crew
          - dir: tooling/pytest-agentic-crew
            name: pytest-agentic-crew
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v7

      - name: Build package
        run: uv build
        working-directory: ${{ matrix.dir }}

      - name: Publish to PyPI
        run: uv publish
        working-directory: ${{ matrix.dir }}

  # ===========================================================================
  # Docker multi-platform build & push
  # ===========================================================================
  docker-publish:
    name: Docker image
    runs-on: ubuntu-latest
    needs: verify
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
