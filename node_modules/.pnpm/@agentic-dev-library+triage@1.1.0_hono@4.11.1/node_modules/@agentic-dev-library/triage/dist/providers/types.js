/**
 * Triage Provider Abstraction
 *
 * Defines common interfaces for issue tracking providers.
 * Allows agentic-triage to work with multiple backends:
 * - GitHub Issues
 * - Beads (local-first, AI-native)
 * - Jira (enterprise)
 * - Linear (modern)
 *
 * Similar to how AI providers work,
 * agentic-triage has multiple triage/issue providers.
 */
// =============================================================================
// Utility Functions
// =============================================================================
/**
 * Map provider priority values to normalized priority
 */
export function normalizePriority(value) {
    if (typeof value === 'string') {
        const lower = value.toLowerCase();
        if (lower === 'critical' || lower === 'highest' || lower === 'p0')
            return 'critical';
        if (lower === 'high' || lower === 'p1')
            return 'high';
        if (lower === 'medium' || lower === 'medium' || lower === 'p2')
            return 'medium';
        if (lower === 'low' || lower === 'p3')
            return 'low';
        if (lower === 'lowest' || lower === 'backlog' || lower === 'p4')
            return 'backlog';
        return 'medium';
    }
    // Numeric: 0=critical, 1=high, 2=medium, 3=low, 4+=backlog
    if (value === 0)
        return 'critical';
    if (value === 1)
        return 'high';
    if (value === 2)
        return 'medium';
    if (value === 3)
        return 'low';
    return 'backlog';
}
/**
 * Map normalized priority to numeric value (0 is highest)
 */
export function priorityToNumber(priority) {
    switch (priority) {
        case 'critical':
            return 0;
        case 'high':
            return 1;
        case 'medium':
            return 2;
        case 'low':
            return 3;
        case 'backlog':
            return 4;
    }
}
/**
 * Map provider status strings to normalized status
 */
export function normalizeStatus(value) {
    const lower = value.toLowerCase().replace(/[_-]/g, '');
    if (lower === 'open' || lower === 'new' || lower === 'todo')
        return 'open';
    if (lower === 'inprogress' || lower === 'active' || lower === 'doing')
        return 'in_progress';
    if (lower === 'blocked' || lower === 'waiting')
        return 'blocked';
    if (lower === 'closed' || lower === 'done' || lower === 'resolved')
        return 'closed';
    return 'open';
}
/**
 * Map provider type strings to normalized type
 */
export function normalizeType(value) {
    const lower = value.toLowerCase();
    if (lower === 'bug' || lower === 'defect')
        return 'bug';
    if (lower === 'feature' || lower === 'enhancement' || lower === 'story')
        return 'feature';
    if (lower === 'task' || lower === 'subtask')
        return 'task';
    if (lower === 'epic' || lower === 'initiative')
        return 'epic';
    if (lower === 'chore' || lower === 'maintenance')
        return 'chore';
    if (lower === 'docs' || lower === 'documentation')
        return 'docs';
    return 'task';
}
//# sourceMappingURL=types.js.map