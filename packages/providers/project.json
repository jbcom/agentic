{
  "name": "providers",
  "tags": ["lang:ts", "scope:providers"],
  "targets": {
    "build": {
      "command": "tsc",
      "options": { "cwd": "{projectRoot}" }
    },
    "lint": {
      "command": "biome lint src/",
      "options": { "cwd": "{projectRoot}" }
    },
    "typecheck": {
      "command": "tsc --noEmit",
      "options": { "cwd": "{projectRoot}" }
    }
  }
}
